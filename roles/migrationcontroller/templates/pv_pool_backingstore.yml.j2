apiVersion: noobaa.io/v1alpha1
kind: BackingStore
metadata:
  finalizers:
  - noobaa.io/finalizer
  labels:
    app: noobaa
  name: mcg-pv-pool-bs
  namespace: {{ noobaa_namespace }}
spec:
  pvPool:
    numVolumes: {{ noobaa_pv_pool_pv_quantity }}
    resources:
      requests:
        storage: {{ noobaa_pv_pool_pv_size }}
    storageClass: {{ noobaa_pv_pool_pv_storageclass }}
  type: pv-pool
